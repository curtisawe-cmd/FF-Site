# Netlify configuration for FF-Site
# Static site — a single index.html served from the repo root. No build step.

[build]
  publish = "."
  command = ""

# Serve the SPA-style app: any unknown path falls back to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
